---
import SvelteDisplay from "./SvelteDisplay.svelte";
import VueIncrement from "./VueIncrement.vue";
import ReactDecrement from "./ReactDecrement.tsx";
// import SvelteReset from "./SvelteReset.svelte";
import AstroReset from "./AstroReset.astro";
---

<div
  class="text-beige shadow-beige/20 fixed bottom-2 left-2 flex flex-col items-center gap-2 rounded-lg bg-gray-800/80 p-3 font-mono shadow-xs backdrop-blur-sm"
>
  <p class="text-xl leading-tight font-bold">I'm persistent</p>
  <!-- <p class="text-sm font-bold leading-none">(And have some suprises 🤭)</p> -->
  <div class="flex items-center gap-2 text-lg font-bold">
    <!-- <SvelteReset client:load /> -->
    <AstroReset />
    <ReactDecrement client:load />
    <SvelteDisplay client:load />
    <VueIncrement client:load />
  </div>
</div>
